# Edge Functions

Server-side product operations belong here. The first end-to-end slice should add
an `adapt` Edge Function that authenticates the caller, validates the request,
calls the configured AI provider, and persists the result.

No AI provider is connected in this foundation.
